# All files in the target directory (build artifacts)
target/

# Compiled binaries (except final output binary)
!/target/x86_64-unknown-none/
!/target/release/
!/target/debug/

# All files within release and debug except the binary
!/target/x86_64-unknown-none/*
/target/release/*
/target/debug/*

# Un-ignore only binary file
!/target/x86_64-unknown-none/
!/target/release/crust
!/target/debug/crust

# Package config files
**/*.rs.bk
Cargo.lock
Cargo.toml.lock

# Temporary files generated by editors or the OS
*.swp
*.swo
*.DS_Store
*.tmp
*~
*.bak
*.log

# The `cargo` and `rustc` command history file
.cargo-lock

# Coverage files (if using code coverage tools)
.coverage/

# Rust standard coverage files
*.profraw

# Rust Language Server (RLS) data
.rls/
